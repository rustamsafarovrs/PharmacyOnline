<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowNoTitle">true</item>

        <item name="bottomSheetDialogTheme">@style/AppBottomSheetDialogTheme</item>
    </style>

    <style name="TextInputLayout" parent="Widget.MaterialComponents.TextInputLayout.FilledBox">
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:maxLines">1</item>
        <item name="android:textStyle">bold</item>
        <item name="android:singleLine">true</item>
        <item name="android:background">@color/white</item>
        <item name="boxStrokeColor">@color/white</item>
        <item name="boxBackgroundColor">@color/white</item>
        <item name="background">@color/white</item>
        <item name="boxStrokeWidth">0dp</item>
        <item name="hintEnabled">false</item>
        <item name="android:textCursorDrawable">@color/colorAccent</item>

        <item name="color">@color/colorAccent</item>

        <item name="android:theme">@style/TextInputLayoutTheme</item>

    </style>

    <style name="TextInputLayoutTheme" parent="AppTheme">
        <item name="colorControlNormal">@color/colorAccent</item>
        <item name="colorControlActivated">@color/colorAccent</item>
        <item name="colorControlHighlight">@color/colorAccent</item>
        <item name="colorPrimary">@color/colorAccent</item>
        <item name="colorPrimaryDark">@color/colorAccent</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorSurface">@color/transparent</item>
    </style>

    <style name="Widget.MaterialComponents.Button.TextButton.Rectangle">
        <item name="backgroundTint">@color/bt_state</item>
        <item name="android:padding">0dp</item>
        <item name="android:background">@color/colorAccent</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <!-- Text input layout    -->
    <style name="MyTextInputLayout" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox">
        <item name="boxStrokeColor">@color/colorAccent</item>
        <item name="colorControlNormal">@color/colorAccent</item>
        <item name="colorControlActivated">@color/colorAccent</item>
        <item name="hintTextColor">@color/colorAccent</item>
        <item name="errorEnabled">true</item>
    </style>

    <!--Input field style-->
    <style name="MyEditText" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox">
        <item name="colorControlNormal">@color/colorAccent</item>
        <item name="colorControlActivated">@color/colorAccent</item>
    </style>

    <!--    Bottom Sheet-->
    <style name="AppBottomSheetDialogTheme" parent="Theme.Design.Light.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/AppModalStyle</item>
    </style>

    <style name="AppModalStyle" parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">@drawable/rounded_dialog</item>
    </style>

    <style name="ChipCustomStyle" parent="Widget.MaterialComponents.Chip.Choice">
        <!--        <item name="checkedIconEnabled">false</item>-->
        <!--        <item name="android:background">@color/chip_background</item>-->
        <!--        <item name="checkedIcon">@null</item>-->
        <!--        <item name="android:checkable">true</item>-->

        <!-- Chip background color selector -->
        <item name="chipBackgroundColor">@color/chip_background</item>
        <!-- Border color -->
        <item name="chipStrokeColor">@color/md_black_1000</item>

        <!-- Chip text color selector -->
        <item name="android:textColor">@color/mtrl_choice_chip_text_color</item>
        <!-- Chip close icon color selector -->
        <item name="closeIconTint">@color/mtrl_chip_close_icon_tint</item>
    </style>

</resources>
